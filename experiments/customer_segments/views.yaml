views:
  common:
    - name: model_comparison
      enabled: true
      scope: candidates
      params:
        metric: silhouette

  clustering:
    - name: elbow_curve
      enabled: true
      scope: candidates
    - name: silhouette_curve
      enabled: true
      scope: candidates
    - name: cluster_scatter
      enabled: true
      scope: best
      params:
        features: [annual_income, spending_score]
    - name: cluster_size
      enabled: true
      scope: best
    - name: k_distance
      enabled: true
      scope: best
      params:
        n_neighbors: 5
        eps: 0.55
