name: salary_regression
task: regression

data:
  path: data/raw/salary_regression.csv
  target: annual_salary
  random_state: 42

outputs:
  root: reports
  save_model: true
  save_predictions: true
  figures: true

selection:
  primary_metric: rmse
  direction: minimize
