name: iris_baseline
task: classification

data:
  path: data/raw/iris.csv
  target: target
  random_state: 42

outputs:
  root: reports
  save_model: true
  save_predictions: true
  figures: true

selection:
  primary_metric: f1_macro
  direction: maximize
